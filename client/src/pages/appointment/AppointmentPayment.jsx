import React from "react";

function AppointmentPayment() {
  const handleSubmit = async (e) => {
    e.preventDefault();
  };
  return <div>AppointmentPayment Page</div>;
}

export default AppointmentPayment;
